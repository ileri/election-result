<%= form_with(model: party, local: true) do |form| %>
  <% if party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
      <% party.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :party_name %>
  </div>

  <div class="field">
    <%= form.label :logo %>
    <%= form.text_field :logo, id: :party_logo %>
  </div>

  <div class="field">
    <%= form.label :alliance_id %>
    <%= form.text_field :alliance_id, id: :party_alliance_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
